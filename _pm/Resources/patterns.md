# Patterns

Cross-project patterns and references. Add to this only when something is worth keeping for >1 project.

- **Strangler Fig** for big-file refactors (AGENTS.md Rule 15)
- **Outcome-oriented execution** for migrations (AGENTS.md Rule 12)
- **Subtract before add** — for refactors, list the noise first (AGENTS.md Rule 6)
