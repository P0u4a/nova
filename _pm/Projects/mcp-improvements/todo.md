# todo — mcp-improvements

Remaining items (low priority):

- [ ] **#9** Tool discovery token cost (P3)
  - Server grouping in system prompt
  - Tool name collision check
  - Lazy schema fetch

- [ ] **#10** TUI graceful shutdown (P3)
  - disconnect (not just disable)
  - pause()/resume()
  - SIGTERM → 5s → SIGKILL
