# todo — mcp-improvements

Önerilen sıra (hızlı feedback → kritik debt):

- [ ] **#6** Transport test coverage — `parseMessage` + roundtrip + malformed
- [ ] **#2** `syncFromConfigEx` reconciliation — removed server'ları temizle
- [ ] **#3** `callTool` refactor — JSON escape + allocation azalt
- [ ] **#8** Manager validation — command/url null, server name uniqueness
