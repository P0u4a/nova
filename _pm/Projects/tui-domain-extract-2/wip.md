# tui-domain-extract-2 — In progress

One item at a time.

## Status

Phase 1 (queue management) committed and pushed. `tui.zig` 4141 → **4097** (-44).
`queue.zig` +83 lines. `zig build && zig build test` pass.
