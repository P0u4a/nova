"""Your reusable helpers live here.

When you notice yourself writing the same snippet more than once, save it as a
module in this directory (``.nova/nova/tools/<name>.py``) and import it with
``from nova.tools.<name> import ...``. The package is installed editable, so
new modules are importable immediately.
"""
